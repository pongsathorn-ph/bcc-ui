apiVersion: v1
kind: Service
metadata:
  name: demo-ui-service
  labels:
    app: demo-ui-app
spec:
  ports:
    - name: demo-ui-app
      port: 80
      targetPort: 80
  selector:
    app: demo-ui-app
  type: ClusterIP
